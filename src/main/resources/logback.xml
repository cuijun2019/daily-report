<?xml version="1.0" encoding="UTF-8"?>
<configuration>
	<!-- 日志存放的路径 ,默认为tomcat下面的logs -->
	<property name="USER_HOME" value="${catalina.base}" />

    <appender name="Console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%date{HH:mm:ss.SSS} [%thread] %-5level [%logger{36}] - %msg%n</pattern>
        </encoder>
    </appender>
    <!-- 输出到文件 -->
	<appender name="RollingFile" class="ch.qos.logback.core.rolling.RollingFileAppender">
		<rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
			<!-- 按天分文件夹存放 ，并压缩 -->
			<fileNamePattern>${USER_HOME}/logs/ltemr/%d{yyyy-MM-dd}/log.%i.txt</fileNamePattern>
			<timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
				<!-- 每个文件的最大值 5MB,超过则编号,生成新文件 -->
				<maxFileSize>10MB</maxFileSize>
			</timeBasedFileNamingAndTriggeringPolicy>
			<!-- 例如，假设每天滚动，且 maxHistory 是 6，则只 保留最近 6 天的归档文件，删除之前的文件。 -->
			<maxHistory>7</maxHistory>
		</rollingPolicy>
		<encoder>
			<pattern>%date{HH:mm:ss.SSS} %-5level %logger{40} - %msg%n</pattern>
		</encoder>
	</appender>
    
    <logger name="java.sql" level="DEBUG" />
	<!-- MyBatis3 的包路径 SQL信息被这个logger打印-->
	<logger name="org.apache.ibatis" level="DEBUG" />
	<!-- MyBatis-spring插件的包路径 自动化加载信息的logger -->
	<logger name="org.mybatis.spring" level="INFO"/>
    <logger name="com.etone" level="DEBUG" />
    <!-- 这个类打印的太多warn级别日志 -->
    <logger name="com.atomikos.icatch.imp.ActiveStateHandler" level="ERROR" />

    <root level="INFO">
        <appender-ref ref="Console"/>
        <appender-ref ref="RollingFile"/>
    </root>
</configuration>